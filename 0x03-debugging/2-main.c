#include <stdio.h>
#include "holberton.h"

/**
 * main - prints the largest of 3 integers
 * Return: 0
 */

int main(void)
{
	int a, b, c;
	int n;

	a = 972;
	b = -98;
	c = 0;

	n = a, b, c;

	printf("%d is the largest number\n", n);

	return (0);
}
