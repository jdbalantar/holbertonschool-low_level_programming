#include "holberton.h"
#include <stdio.h>

/**
 * print_last_digit - print absolute value
 * Return: int
 * @num: input character
 */

int print_last_digit(int num)
{
	int x = (num % 10);

	return (x);
}
