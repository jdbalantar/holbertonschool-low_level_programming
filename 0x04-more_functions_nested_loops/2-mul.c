#include "holberton.h"
/**
 * mul - Write a function that multiplies two integers.
 * Return: int
 * @a: first integer
 * @b: second integer
 */
int mul(int a, int b)
{
	int m = (a * b);

	return (m);
}
